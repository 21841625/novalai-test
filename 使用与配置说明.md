# AutoNovel 中文网文自动创作工具 - 完整使用说明
这是专为**中文网文作者**定制的AI自动写作工具，支持三种创作模式，一键生成从设定→大纲→正文→评估→发布的全流程内容，适配番茄/起点等主流网文平台，拒绝口水文、保护原创设定。

---

## 一、核心特性
1. **三种创作模式**（一键切换）
   - ✅ **平衡模式（默认推荐）**：有质感的网文，兼顾追读节奏+内容深度，无俗套模板
   - ✅ **爽文模式**：标准番茄爽文，黄金三章、密集爽点、强钩子
   - ✅ **文学模式**：偏文学创作，重人物弧光、三幕式结构
2. **全自动化流水线**：不用手动跑单个脚本，一键生成全部内容
3. **中文评估体系**：`evaluate.py` 已全中文提示词，AI评估符合网文审稿标准
4. **发布适配**：一键导出番茄小说纯文本格式
5. **合规检测**：自动检查敏感词，确保内容过审

---

## 二、前置准备（必看）
### 1. 项目文件清理（你之前问的删除规则）
#### ✅ 绝对不能删（核心配置文件）
```
项目根目录/
├─ voice.md       # 写作风格定义（必须保留）
├─ CRAFT.md       # 写作技巧规则（必须保留）
└─ .env           # API密钥+模式配置（必须保留）
```

#### 🗑️ 可以直接删除（历史无用文件）
根目录下的旧生成文件，删了不影响运行：
`world.md / characters.md / outline.md / canon.md / chapters/ 文件夹`
英文说明文档（不需要可删）：`ANTI-PATTERNS.md、WORKFLOW.md` 等

---

### 2. 环境配置
1. 确保安装 Python 3.8+
2. 安装依赖（如有需要）：
```bash
pip install requests python-dotenv
```

---

### 3. .env 配置文件（核心！）
在项目根目录创建/修改 `.env` 文件，填写以下内容：
```env
# 1. API 配置（二选一，填写你的密钥）
DEEPSEEK_API_KEY=你的深度求索密钥
# ANTHROPIC_API_KEY=你的Claude密钥

# 2. 创作模式切换（三选一，修改等号后面的值）
AUTONOVEL_MODE=balance      # 平衡模式（推荐）
# AUTONOVEL_MODE=shuangwen  # 爽文模式
# AUTONOVEL_MODE=literature # 文学模式
```

---

## 三、最快使用方式：一键全自动流水线
**一行命令，跑完所有流程**（设定→大纲→正文→润色→评估）：
```bash
python run_pipeline.py
```

### 流水线高级用法
1. 只重新生成第5章：
```bash
python run_pipeline.py --only-chapter 5
```
2. 从生成大纲开始重新跑：
```bash
python run_pipeline.py --from-step gen_outline
```
3. 跳过已完成的内容（断点续跑，默认开启）：
```bash
python run_pipeline.py --skip-done
```

---

## 四、分步命令（手动控制每一步）
如果不想一键跑，可单独执行每个步骤：
### 1. 生成核心设定
```bash
python gen_world.py       # 生成世界观
python gen_characters.py  # 生成角色设定
python gen_outline.py     # 生成章节大纲
```

### 2. 生成&润色正文
```bash
python draft_chapter.py 1    # 生成第1章草稿
python revise_chapter.py 1   # 润色第1章
```

### 3. 中文AI评估（全中文提示词，已优化）
```bash
# 评估世界观/大纲/角色等基础设定
python evaluate.py --phase foundation

# 评估第1章正文（评分+修改建议）
python evaluate.py --chapter 1

# 评估整本小说
python evaluate.py --full
```

### 4. 读者评审团（多视角评估）
```bash
python reader_panel.py --chapter 1
```
包含：网文读者+文学爱好者+反俗套检测，三方打分

### 5. 发布工具（新增）
```bash
# 1. 导出番茄小说纯文本发布版（自动去格式，直接复制发布）
python export_tomato.py

# 2. 全文章节敏感词合规检测
python content_check.py
```

---

## 五、输出文件说明
所有生成的内容都在 `out_doc/` 文件夹，结构清晰：
```
out_doc/
├─ md_file/            # 设定文件
│  ├─ 世界设定.md
│  ├─ 角色设定.md
│  └─ 章节大纲.md
├─ chapters/           # 正文章节（润色后最终版）
└─ 完整小说.md         # 整本小说合并版
```
番茄发布版：`out_doc/番茄发布版.txt`

---

## 六、三种模式区别（一目了然）
| 模式 | 适用场景 | 核心规则 |
|------|----------|----------|
| 平衡模式 | 精品网文、无限流、原创脑洞 | 弹性开篇、情绪钩子、无俗套、保护原创 |
| 爽文模式 | 番茄/七猫爽文 | 黄金三章、强爽点、强追读钩子 |
| 文学模式 | 出版级文学、慢热文 | 三幕结构、人物弧光、文学质感 |

---

## 七、常见问题
### 1. 运行报错：文件找不到
- 检查是否保留了 `voice.md` 和 `CRAFT.md`
- 不要删除项目根目录的核心脚本

### 2. 评估还是英文？
- 我已将 `evaluate.py` 全量改为**中文提示词**，直接用即可

### 3. 切换模式不生效？
- 修改 `.env` 后**重新运行命令**，无需重启电脑
- 执行 `python run_pipeline.py` 会自动打印当前模式

### 4. 敏感词检测太严/太松？
- 打开 `content_check.py`，手动修改 `SENSITIVE_WORDS` 词库

---

## 八、总结（最简操作流程）
1. 配置 `.env` → 选择创作模式
2. 运行 `python run_pipeline.py`
3. 等待生成完成
4. 运行 `python export_tomato.py` 导出发布
5. 运行 `python content_check.py` 检测合规

全程无需手动修改代码，纯傻瓜式操作！