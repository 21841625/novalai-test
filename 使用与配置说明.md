# autonovel 项目使用与配置说明

## 项目概述

`autonovel` 是一个由 AI 代理驱动的自动化小说创作管道，能够从种子概念（seed concept）生成完整的小说，包括：
- 打印就绪的 PDF 格式
- ePub 电子书格式
- 多语音有声读物
- 响应式落地页

该项目灵感来源于 `karpathy/autoresearch`，将"修改-评估-保留/丢弃"循环应用于小说创作领域。

---

## 快速开始

```bash
# 克隆项目并进入目录
git clone <repo-url> && cd autonovel

# 复制环境变量模板并配置 API 密钥
cp .env.example .env

# 安装依赖（使用 uv 包管理器）
uv sync

# 生成种子概念（或手动编写 seed.txt）
uv run python seed.py

# 运行完整管道
uv run python run_pipeline.py --from-scratch
```

---

## 环境配置

### API 密钥配置

项目支持两种 API 提供商：**DeepSeek**（推荐，国内可用）和 **Anthropic**。复制 `.env.example` 到 `.env` 并填写：

| 服务 | 密钥名称 | 必填 | 用途 |
|------|----------|------|------|
| DeepSeek | `DEEPSEEK_API_KEY` | **是（使用DeepSeek时）** | 写作、评估、审核（v4 Flash/Pro） |
| Anthropic | `ANTHROPIC_API_KEY` | **是（使用Anthropic时）** | 写作、评估、审核（Sonnet + Opus） |
| fal.ai | `FAL_KEY` | 否 | 封面艺术和装饰生成（Nano Banana 2） |
| ElevenLabs | `ELEVENLABS_API_KEY` | 否 | 多语音有声读物生成 |

### API 提供商配置

在 `.env` 中设置 API_PROVIDER 来切换提供商：

```ini
# 使用 DeepSeek（推荐，国内可用）
AUTONOVEL_API_PROVIDER=deepseek
DEEPSEEK_API_KEY=${DEEPSEEK_API_KEY}  # 支持读取系统环境变量
AUTONOVEL_API_BASE_URL=https://api.deepseek.com/v1

# 使用 Anthropic（需海外网络）
# AUTONOVEL_API_PROVIDER=anthropic
# ANTHROPIC_API_KEY=sk-ant-...
# AUTONOVEL_API_BASE_URL=https://api.anthropic.com
```

### 模型配置

在 `.env` 中配置使用的模型：

| 配置项 | DeepSeek 默认值 | Anthropic 默认值 | 说明 |
|--------|----------------|------------------|------|
| `AUTONOVEL_WRITER_MODEL` | `deepseek-v4-flash` | `claude-sonnet-4-6` | 用于起草和修订的模型 |
| `AUTONOVEL_JUDGE_MODEL` | `deepseek-v4-flash` | `claude-sonnet-4-6` | 用于评估的模型（建议与写作模型不同） |
| `AUTONOVEL_REVIEW_MODEL` | `deepseek-v4-pro` | `claude-opus-4-6` | 用于深度审核的模型 |
| `AUTONOVEL_API_BASE_URL` | `https://api.deepseek.com/v1` | `https://api.anthropic.com` | API 基础 URL |

---

## 项目架构

### 文件结构

```
├── 框架文件（可复用，在 master 分支）：
│   ├── program.md        # 各阶段代理指令
│   ├── CRAFT.md          # 写作技巧（情节、角色、世界、散文）
│   ├── ANTI-SLOP.md      # 词语级 AI 检测
│   ├── ANTI-PATTERNS.md  # 结构级 AI 模式检测
│   ├── PIPELINE.md       # 完整自动化规范
│   └── WORKFLOW.md       # 分步人工指南

├── 模板文件（每部小说在分支上填充）：
│   ├── voice.md          # 写作风格指南（第1部分：规则；第2部分：发现）
│   ├── world.md          # 世界设定模板
│   ├── characters.md     # 角色注册表模板
│   ├── outline.md        # 章节大纲模板
│   ├── canon.md          # 硬事实数据库
│   ├── MYSTERY.md        # 核心谜团（作者专用）
│   └── state.json        # 管道状态追踪器

├── 排版文件：
│   └── typeset/
│       ├── novel.tex     # LaTeX 模板
│       ├── build_tex.py  # 章节转 LaTeX
│       └── epub_*        # ePub 元数据、CSS 和前言

├── 艺术文件：
│   ├── audiobook_voices.json  # 角色到 ElevenLabs 语音映射
│   └── landing/index.html     # 响应式落地页模板

├── 工具脚本（27个 Python 脚本）：
│   ├── seed.py           # 生成种子概念
│   ├── gen_world.py      # 生成世界设定
│   ├── gen_characters.py # 生成角色
│   ├── gen_outline.py    # 生成大纲
│   ├── draft_chapter.py  # 起草单章
│   ├── evaluate.py       # 评估
│   ├── gen_revision.py   # 修订章节
│   ├── review.py         # Opus 审核
│   ├── gen_art.py        # 艺术生成
│   ├── gen_audiobook.py  # 有声读物生成
│   └── run_pipeline.py   # 完整管道编排器
```

---

## 管道流程

### Phase 0: 设置

```bash
# 创建小说分支
git checkout -b autonovel/my-novel

# 确保 .env 配置了 API 密钥（DeepSeek 或 Anthropic）
# 确保 seed.txt 存在且内容足够具体
```

### Phase 1: 基础构建

**输入**: `seed.txt`
**输出**: `world.md`, `characters.md`, `outline.md`, `voice.md`, `canon.md`, `MYSTERY.md`
**退出条件**: `foundation_score > 7.5` 且 `lore_score > 7.0`

循环过程：
1. `gen_world.py` → 生成世界设定
2. `gen_characters.py` → 生成角色注册表
3. `gen_outline.py` → 生成大纲第1部分
4. `gen_outline_part2.py` → 生成大纲第2部分（伏笔记录）
5. 语音发现：写5段不同风格的试验段落，选择最佳
6. 定义 `MYSTERY.md`
7. `gen_canon.py` → 生成硬事实数据库
8. `evaluate.py --phase=foundation` → 评估

### Phase 2: 初稿

**输入**: 所有基础文档
**输出**: `chapters/ch_01.md` 至 `chapters/ch_NN.md`
**退出条件**: 所有章节评分 > 6.0

```bash
# 自动运行所有章节
uv run python run_drafts.py

# 或手动单章起草
uv run python draft_chapter.py --chapter=5
```

### Phase 3: 修订

这是提升质量的关键阶段，通常需要 3-6 个循环，当分数稳定时停止。

```bash
# 对抗性编辑分析
uv run python adversarial_edit.py all

# 章节对比（Elo 排名）
uv run python compare_chapters.py

# 应用剪切
uv run python apply_cuts.py all --types OVER-EXPLAIN REDUNDANT

# 读者面板评估（4个角色）
uv run python reader_panel.py

# 生成修订摘要
uv run python gen_brief.py --auto

# 根据摘要重写章节
uv run python gen_revision.py 5 briefs/ch05.md
```

### Phase 3b: Opus 审核循环

使用 Claude Opus 进行深度散文级审核：

```bash
# 运行审核
uv run python review.py --output reviews.md

# 解析可操作项
uv run python review.py --parse
```

**停止条件**：
- 无重大未限定问题
- >50% 的问题是限定/模糊的
- 仅找到 ≤2 个问题

### Phase 4: 导出

```bash
# 重建文档
uv run python build_outline.py
uv run python build_arc_summary.py

# 生成 LaTeX 内容
uv run python typeset/build_tex.py

# 编译 PDF（需要 tectonic）
cd typeset && tectonic novel.tex
```

---

## 核心工具详解

### 基础构建工具

| 工具 | 用途 |
|------|------|
| `seed.py` | 生成种子概念 |
| `gen_world.py` | 从种子生成世界设定 |
| `gen_characters.py` | 从种子+世界生成角色注册表 |
| `gen_outline.py` | 生成带节奏和伏笔的大纲 |
| `gen_outline_part2.py` | 生成伏笔记录 |
| `gen_canon.py` | 生成硬事实数据库 |
| `voice_fingerprint.py` | 语音分析和发现 |

### 起草工具

| 工具 | 用途 |
|------|------|
| `draft_chapter.py` | 按反模式规则起草单章 |
| `run_drafts.py` | 批量顺序章节起草 |

### 评估工具

| 工具 | 用途 |
|------|------|
| `evaluate.py` | 机械检测 + LLM 评估 |
| `adversarial_edit.py` | "删减 500 词"分析 |
| `compare_chapters.py` | 章节对比 Elo 锦标赛 |
| `reader_panel.py` | 4角色小说级评估 |
| `review.py` | Opus 双角色审核 |

### 修订工具

| 工具 | 用途 |
|------|------|
| `gen_brief.py` | 从反馈自动生成修订摘要 |
| `gen_revision.py` | 根据修订摘要重写章节 |
| `apply_cuts.py` | 批量应用对抗性剪切 |

### 艺术与封面工具

| 工具 | 用途 |
|------|------|
| `gen_art.py` | 艺术管道：风格、筛选、装饰、矢量化 |
| `gen_art_directions.py` | 生成多种艺术风格供筛选 |
| `gen_cover_composite.py` | 封面艺术文字叠加 |
| `gen_cover_print.py` | 打印就绪全包裹封面 |

### 有声读物工具

| 工具 | 用途 |
|------|------|
| `gen_audiobook_script.py` | 解析章节为带说话者的脚本 |
| `gen_audiobook.py` | 通过 ElevenLabs 生成多语音音频 |

### 编排工具

| 工具 | 用途 |
|------|------|
| `run_pipeline.py` | 完整管道编排器 |
| `build_arc_summary.py` | 从章节重新生成弧摘要 |
| `build_outline.py` | 从章节重新生成大纲 |

---

## 运行方式

### 运行完整管道

```bash
# 从头开始运行所有阶段
uv run python run_pipeline.py --from-scratch

# 运行特定阶段
uv run python run_pipeline.py --phase foundation
uv run python run_pipeline.py --phase drafting
uv run python run_pipeline.py --phase revision --max-cycles 5
uv run python run_pipeline.py --phase export
```

### 手动运行各工具

```bash
# 评估
uv run python evaluate.py --phase=foundation   # 评分基础文档
uv run python evaluate.py --chapter=5          # 评分单章
uv run python evaluate.py --full               # 评分整部小说

# 修订
uv run python adversarial_edit.py all          # 分析所有章节
uv run python apply_cuts.py all --types OVER-EXPLAIN REDUNDANT
uv run python reader_panel.py                  # 4角色评估
uv run python review.py                        # Opus 审核

# 艺术（需要 FAL_KEY）
uv run python gen_art.py curate cover --n=6    # 生成封面变体
uv run python gen_art.py pick cover 3          # 选择变体
uv run python gen_art.py ornaments-all         # 生成章节装饰

# 有声读物（需要 ELEVENLABS_API_KEY）
uv run python gen_audiobook_script.py          # 解析章节
uv run python gen_audiobook.py --list-voices   # 浏览语音
uv run python gen_audiobook.py                 # 生成所有
```

---

## 评分系统

### 评估维度

- **主题连贯性**：种子概念的中心问题是否贯穿始终
- **语音一致性**：是否保持统一的叙事视角和词汇风格
- **伏笔设置**：每个伏笔是否有对应的回收
- **节奏控制**：情节推进的快慢是否得当
- **角色塑造**：角色是否立体、有深度

### 停止阈值

| 阶段 | 阈值 |
|------|------|
| 基础构建 | foundation_score > 7.5 |
| 基础构建 | lore_score > 7.0 |
| 初稿 | 单章 score > 6.0 |
| 修订循环 | Δ < 0.5（连续2个循环） |

---

## 五个层级

小说由五个共同演进的层级组成：

```
  第5层: voice.md          — 写作方式（HOW）
  第4层: world.md          — 存在内容（WHAT）
  第3层: characters.md     — 行动者（WHO）
  第2层: outline.md        — 发生事件（WHAT HAPPENS）
  第1层: chapters/ch_NN.md — 实际散文（THE ACTUAL PROSE）
  跨层: canon.md           — 真实事实（WHAT IS TRUE）
```

---

## 两个免疫系统

1. **机械检测** (`evaluate.py`)：正则扫描禁用词、陈词滥调、"展示而非讲述"违规、句子长度均匀性

2. **LLM 评估** (`evaluate.py`)：评分散文质量、语音一致性、角色独特性、节奏覆盖

---

## 时间预估

| 阶段 | API 时间 | 说明 |
|------|----------|------|
| Phase 1（基础） | 2-4 小时 | 5-15 次迭代 |
| Phase 2（初稿） | 8-16 小时 | 23-30 章 |
| Phase 3（修订） | 4-8 小时 | 3-6 个循环 |
| Phase 4（导出） | 30 分钟 | PDF/ePub 生成 |
| **总计** | **15-30 小时** | 75,000 词小说 |

---

## 技术栈

- **语言**: Python 3.12+
- **包管理器**: uv
- **依赖**: httpx, python-dotenv
- **排版**: LaTeX (tectonic)
- **AI 服务**: DeepSeek v4 (推荐) / Anthropic Claude, fal.ai, ElevenLabs
- **统一 API 客户端**: `api_client.py` - 支持动态切换 API 提供商

---

## 注意事项

1. **API 成本**：完整运行一个小说可能产生较高的 API 费用，建议在测试阶段使用较低成本的模型
2. **质量平衡**：初稿阶段追求进度而非完美（6.0分即可），修订阶段才追求高质量
3. **人工干预**：虽然管道是自动化的，但人工审核和调整仍然很重要
4. **版本控制**：建议为每部小说创建独立分支，便于管理不同版本

---

## 参考资料

- [karpathy/autoresearch](https://github.com/karpathy/autoresearch) — 自动化研究循环
- Brandon Sanderson 的写作讲座（魔法定律、角色滑块）
- K.M. Weiland 的《Creating Character Arcs》
- Blake Snyder 的《Save the Cat》
- Ursula K. Le Guin 的 "From Elfland to Poughkeepsie"