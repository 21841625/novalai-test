一、核心规则（必看）
只用你手动写的 seed.txt（不用 AI 灵感生成器）

只运行 3 个框架生成脚本（不跑正文、不跑评估、不跑润色）
所有框架文件都会生成在 out_doc/md_file/，可自由编辑修改
绝对不执行 run_pipeline.py（这个是全流程，会生成正文）

# 0.创建灵感文件
1.自动创建,执行python create_seed.py
2.手动创建, 新建 seed.txt 文件, 写入灵感内容

# 1. 生成 世界观设定
python gen_world.py

# 2. 生成 角色设定
python gen_characters.py

# 3. 生成 完整章节大纲
python gen_outline.py

三、生成的框架文件在哪里？
所有核心框架都在这个文件夹：
out_doc/
└─ md_file/
   ├─ 世界设定.md    ✅ 完整世界观、规则、背景
   ├─ 角色设定.md    ✅ 主角、配角、反派设定
   └─ 章节大纲.md    ✅ 全书分卷/章节剧情大纲

四、你可以做的事（完全人工控制）
打开框架文件自由修改
不满意 AI 生成的世界观 / 大纲 / 角色，直接用编辑器改，想怎么改就怎么改
修改完成后，再决定是否生成正文
后续想写正文了，再运行：
bash
运行
python draft_chapter.py 1  # 生成第1章
python draft_chapter.py 2  # 生成第2章
全程不生成正文、不自动创作，完全由你掌控节奏